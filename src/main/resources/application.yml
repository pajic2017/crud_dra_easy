spring:
  datasource:
    username: postgres
    password: test
    url: jdbc:postgresql://localhost:5432/hole


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

